pipeline {
    agent any

    stages {
        stage('Print Environment Variables') {
            steps {
                script {
                    echo env
                }
            }
        }
    }
}
